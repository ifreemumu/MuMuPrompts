# MuMu Prompts 디자인 시스템

사용자가 선택한 **따뜻한 크림색 갤러리형**. 이미지 탐색 → 내용 바꾸기 → 복사가 중심이며, 등록·백업 도구는 관리자에게만 보여준다. 기존 MuMu의 종이색과 노란 포인트를 유지한다.

## 화면 구조

- 갤러리: 짧은 소개, 카테고리 탐색, 결과 수·정렬, 이미지 카드.
- 1024px 이상: 왼쪽 210px 카테고리 목록, 오른쪽 갤러리. 그 이하는 카테고리 펼침 버튼.
- 카드: 모바일 2열, 640px 이상 3열, 1440px 이상 4열. 이미지 원래 비율을 유지하는 masonry 배치.
- 상세: 선택 이미지·입력란을 주 영역으로, 비슷한 이미지는 보조 영역으로 배치. 복사 버튼은 스크롤 중에도 상단에 남긴다.
- 관리자: 공개 목록 / 이미지 미등록 전환과 백업 / 추가 작업을 분리. 목록 전환 시 검색·분류 필터 초기화.

## 토큰

정확한 값의 원본은 `tokens.css`. 새 화면은 색·글꼴을 직접 지정하지 않고 같은 토큰을 사용한다.

| 역할 | CSS 토큰 | 사용 |
| --- | --- | --- |
| 바탕 / 표면 | `--color-paper` / `--color-panel` | 크림색 페이지 / 밝은 카드 |
| 본문 / 보조 글자 | `--color-ink` / `--color-muted` | 녹색 기운의 진한 글자 / 설명 |
| 선택 | `--color-selected` | 활성 카테고리, 밝은 글자와 조합 |
| 주요 동작 | `--color-accent` / `--color-accent-ink` | 노란 복사·추가 버튼 |
| 키보드 포커스 | `--color-focus` | 즉시 표시하는 outline |
| 글꼴 | `--font-display` / `--font-body` | 로컬 Pretendard, 제목은 정체 |
| 간격 | `--space-2xs` … `--space-3xl` | 4 / 8 / 12 / 16 / 24 / 40 / 64 / 96px |

타 구현으로 옮길 때도 위 역할명을 유지한다. 예: JS 테마의 `colors.paper`는 `var(--color-paper)`, `fontFamily.body`는 `var(--font-body)`를 참조한다. 별도 팔레트를 복제하지 않는다.

## 상호작용과 접근성

- `/`: 입력 중이거나 대화상자가 열려 있지 않으면 검색으로 이동. 검색창의 Escape는 검색어 삭제.
- 검색 결과 없음과 자료 자체가 없음은 다른 안내를 제공한다.
- 모바일 카테고리는 선택 후 접히며 버튼으로 포커스를 돌려준다.
- 검색창의 label, 선택 버튼의 aria-pressed, 카테고리 버튼의 aria-expanded를 유지한다.
- 좁은 화면에서도 버튼 문구를 줄바꿈하지 않는다. 터치 환경의 주요 컨트롤은 44px 이상.
- 복사 성공은 버튼의 체크 표시와 문구로 알린다. 오류는 기존 오류 안내를 유지한다.
- 데이터·이미지 저장, OAuth, Drive 게시 코드는 이 디자인 변경의 대상이 아니다.

## 검수 방법

`node --test tests/regression.test.cjs`

데이터를 건드리지 않는 UI 검수는 아래의 별도 localhost 환경을 사용한다. 기본 이미지와 테스트용 미등록 자료만 제공하며 Google 스크립트는 불러오지 않는다.

```text
node tests/ui-server.cjs 8093 admin
node tests/ui-server.cjs 8094 public
```

320 / 375 / 414 / 768 / 1280px에서 갤러리·대화상자의 가로 넘침, 텍스트 대비, 카테고리 접힘, 검색 초기화, 복사 피드백, 공개 목록의 미등록 자료 제외를 확인한다. 실제 Google 로그인과 업로드 복구 여부는 별도 검증이 필요하다.
